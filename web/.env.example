# Exemplo de variáveis do frontend — copie para .env
# IMPORTANTE: apontar SEMPRE para a porta do backend HENRIQUE BARBER
# Backend: http://localhost:3333 (porta 3000 é de outro projeto)
# Em produção (Render), use a URL do backend no Render:
VITE_API_URL=https://barbershop-6655.onrender.com